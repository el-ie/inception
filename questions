
pourquoi docker-compose up ne prend pas en compte la modification des Dockerfile (meme en refesant un docker build -t test .)
du coup --> docker-compose up --build

---------------------------------------------------

faut il faire

	build:
	  context: ./lala/lala
	  dockerfile: Dockerfile  OUI COMME CA
ou
	build: ./lala/lala/Dockerfile NE PAS FAIRE CA 
			sinon le docker-compose up --build ne fonctione plus

---------------------------------------------------
